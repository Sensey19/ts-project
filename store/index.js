import nuxtServerInit from './_nuxtServerInit';

export const state = () => ({
  counter: 0
});

export const mutations = {

};

export const actions = {
  nuxtServerInit
};

export const getters = {
  // isMainData: (state) => state.mainData
};
